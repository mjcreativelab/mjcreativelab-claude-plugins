# コンテキスト管理チェックリスト

Phase 2 Step 2 で使用する。「該当条件」に合致しない項目はスキップする。

| チェック項目 | 確認内容 | 該当条件 |
|---|---|---|
| 読み込みタイミング | 参照ファイルの Read が必要なステップまで遅延されているか。冒頭で一括 Read する指示になっていないか | 参照ファイルが存在するスキル |
| 圧縮耐性 | 長い会話でコンテキスト圧縮が起きた場合の再 Read ルールが明記されているか | 複数ステップを持つタスク型スキル |
| 中間データの外部化 | 処理途中のデータをファイルに書き出してコンテキストから解放する設計があるか | 大量データを扱う or ステップが 5 以上のスキル |
| API レスポンスの絞り込み | API / MCP ツールのレスポンスから必要フィールドだけを抽出するルールがあるか | API 呼び出しを含むスキル |
| バッチ処理設計 | 大量の項目を扱う場合のバッチサイズ・中間保存の設計があるか | リスト・配列を反復処理するスキル |
| サブエージェント委譲 | サブエージェントに渡す情報と返却される結果の範囲が明確か | `context: fork` または Agent ツールを使うスキル |
| テンプレート肥大化リスク | 参照ファイルやテンプレートが将来肥大化した場合の対策（行数制限、分割指針等）があるか | 参照ファイルが 200 行以上、または可変長コンテンツを扱うスキル |
| frontmatter 最適化 | `allowed-tools` が設定されているか。`description` が簡潔かつトリガー条件を含んでいるか | すべてのスキル |
| `disable-model-invocation` | 副作用のある操作（git push, PR 作成, ファイル削除, 外部 API 書き込み等）を含むスキルに設定されているか | 副作用のある操作を含むスキル |
| コンテンツのトークン効率 | 自明なシェルコマンドがコードブロックで冗長に記述されていないか。インライン化や省略で削減できる箇所がないか | コードブロックを 3 つ以上含むスキル |
